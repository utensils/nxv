**SegmentedToggle** — a bordered mono segmented control for a small set of exclusive options. Drives the results rows/cards switch.

```jsx
<SegmentedToggle
  options={[{value:'rows',label:'rows'},{value:'cards',label:'cards'}]}
  value={view}
  onChange={setView}
/>
```
