**ActivityBars** — a compact request-activity sparkline (the "activity · 30m" stats panel). Zero buckets show a dim baseline tick; active buckets scale in green.

```jsx
<ActivityBars data={[0,2,5,1,0,8,12,3,0,4]} />
```
