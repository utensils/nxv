**VersionBadge** — the version tag on package rows and cards, tabular-figured. Tone encodes status.

```jsx
<VersionBadge version="2.7.18" tone="brand" />
<VersionBadge version="1.0.2k" tone="warn" />   {/* pre-flakes */}
<VersionBadge version="1.0.1e" tone="danger" /> {/* insecure */}
```
